You don't need to send anything else yet:

oc get pdb -A -o custom-columns='NAMESPACE:.metadata.namespace,NAME:.metadata.name,MIN:.spec.minAvailable,MAX:.spec.maxUnavailable,ALLOWED:.status.disruptionsAllowed,CURRENT:.status.currentHealthy,DESIRED:.status.desiredHealthy'
oc get pods -n openshift-machine-config-operator | grep machine-config-controller

Then, using the pod name from the second command:

oc logs -n openshift-machine-config-operator <POD_NAME> --since=2h | grep -E '10-64-34-146|10-64-34-142|failed to drain|evict|eviction|PDB'

Once we see the MCO log, I can tell you exactly what is preventing the two nodes from draining and whether we should modify a PDB, move a workload, or take another approach.
