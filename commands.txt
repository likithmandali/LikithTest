Give the CVO a few minutes to reconcile. Then run:

watch -n 10 'oc get clusterversion; echo; oc get co console'

What we want is:

version   4.19.45   True   False

and then specifically:

oc get clusterversion version \
  -o jsonpath='{range .status.conditions[*]}{.type}{"="}{.status}{"  "}{.reason}{"  "}{.message}{"\n"}{end}'

We want:

Available=True
Failing=False
Progressing=False

If Failing flips to False, you're in a much cleaner state for starting 4.20.

If Failing=True remains after ~5–10 minutes

Then investigate the console operator rather than touching monitoring again:

oc get co console -o yaml

More targeted:

oc get co console \
  -o jsonpath='{range .status.conditions[*]}{.type}{"="}{.status}{"  reason="}{.reason}{"  message="}{.message}{"\n"}{end}'

Then:

oc get pods -n openshift-console
oc get pods -n openshift-console-operator

And:

oc logs -n openshift-console-operator \
  deployment/console-operator --tail=100

Also check:

oc get route console -n openshift-console

I would not delete the console pods yet. Right now the operator reports healthy, so give CVO time to catch up.

Red Hat's upgrade documentation specifically uses ClusterOperatorNotAvailable/Failing=True as an example condition to resolve when reviewing upgrade readiness. It also discourages forcing an update past safety guards.

About the Thanos pod

You had:

thanos-ruler-user-workload-1
0/4  ContainerCreating  3s

That's completely unsurprising at 3 seconds old after changing user-workload-monitoring-config. Check it again:

oc get pods -n openshift-user-workload-monitoring

I'd want:

thanos-ruler-user-workload-0   4/4 Running
thanos-ruler-user-workload-1   4/4 Running

before proceeding.

Then do one final pre-upgrade check

Once CVO has Failing=False, run:

oc adm upgrade recommend

and:

oc get co
oc get mcp

Your desired pre-upgrade state is essentially:

ClusterVersion
  Available=True
  Progressing=False
  Failing=False

ClusterOperators
  Available=True
  Progressing=False
  Degraded=False

Monitoring
  Upgradeable=True

MCP master
  UPDATED=True
  UPDATING=False
  DEGRADED=False

MCP worker
  UPDATED=True
  UPDATING=False
  DEGRADED=False

Red Hat also recommends checking OLM-installed Operators for compatibility with the target release before a minor-version update. Given the Operators on this cluster, I would run:

oc get csv -A
oc get subscriptions -A
oc get installplan -A

before kicking off 4.20.

One other important point

You have three offered versions:

4.19.46
4.20.37
4.20.38

You do not automatically need to install 4.19.46 before going to 4.20. The update graph is explicitly offering direct update targets from your current 4.19.45, including 4.20.37 and 4.20.38.

I wouldn't manually use --force or try to construct an update path. Red Hat notes that forcing bypasses cluster-side guards and precondition checks.

So right now: monitoring problem solved → wait for CVO Failing=False → verify Thanos 4/4 → check OLM operators → then proceed with the offered 4.20 update.

Send me the output of:

oc get clusterversion version \
-o jsonpath='{range .status.conditions[*]}{.type}{"="}{.status}{"  "}{.reason}{"  "}{.message}{"\n"}{end}'

oc get pods -n openshift-user-workload-monitoring

oc get csv -A

and I'll check the final upgrade readiness before you click the 4.20 update.
